{
  "name": "mental",
  "version": "1.0.0",
  "private": true,
  "description": "Mind centralization system with MCP integration",
  "scripts": {
    "build": "pnpm -r build",
    "deploy:api": "fly deploy --local-only",
    "deploy:mcp": "source .env.npm.local && pnpm --filter @go-mental/mcp build && cd packages/mcp-server && npm publish --access public --registry https://registry.npmjs.org"
  },
  "devDependencies": {
    "typescript": "^5.3.0",
    "@types/node": "^20.10.0"
  }
}
