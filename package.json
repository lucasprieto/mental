{
  "name": "mental",
  "version": "1.0.0",
  "private": true,
  "description": "Mind centralization system with MCP integration",
  "scripts": {
    "build": "pnpm -r build",
    "deploy": "railway up"
  },
  "devDependencies": {
    "typescript": "^5.3.0",
    "@types/node": "^20.10.0"
  }
}
