{
  "name": "mental",
  "version": "1.0.0",
  "private": true,
  "description": "Mind centralization system with MCP integration",
  "scripts": {
    "build": "pnpm -r build",
    "deploy:api": "koyeb deploy . mental-api/mental-api --archive-builder docker --ports 3000:http --routes /:3000 --env DATABASE_URL=@database-url"
  },
  "devDependencies": {
    "typescript": "^5.3.0",
    "@types/node": "^20.10.0"
  }
}
